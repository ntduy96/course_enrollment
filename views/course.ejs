<!DOCTYPE html>
<html lang="en">

<head>
  <title></title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>
    <%=course_name%>
  </title>
  <link rel="stylesheet" href="/css/bootstrap.css">
</head>

<body class="container text-center">
  <h1>
    <p class="badge">
      <%=course_id%>
    </p>
    <p>
      <%=course_name%>
    </p>
  </h1>
  <!-- display multi-paragraph -->
  <h3 class="text-left">Course Description</h3>
  <div class="text-left">
    <% course_desc.split(/\r\n/g).forEach(function (paragraph) { %>
      <p>
        <%= paragraph %>
      </p>
      <% }) %>
  </div>
  <% if(isEnrolled){ %>
    <form action="/drop" method="post" class="navbar-form">
      <input type="submit" value="Drop" class="btn btn-success btn-lg">
    </form>
    <% } else{ %>
      <form action="/enroll" method="post" class="navbar-form">
        <input type="submit" value="Enroll" class="btn btn-success btn-lg">
      </form>
      <% } %>

</body>

</html>